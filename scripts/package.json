{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "migrate-video-urls": "tsx ./src/migrate-video-urls.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "migrate-audio-urls": "tsx ./src/migrate-audio-urls.ts",
    "post-deploy-smoke-test": "tsx ./src/post-deploy-smoke-test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.830.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "@types/pg": "^8.18.0"
  }
}
